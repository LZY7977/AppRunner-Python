version: 1.0
runtime: python3 
build:
  commands:
    pre-build:
      - echo "pre-build"
    build:
      - pip install pipenv
    post-build:
      - echo "post-build"
run: 
  runtime-version: 3.8.15
  command: python test.py
