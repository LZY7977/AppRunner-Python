version: 1.0
runtime: python311
build:
  commands:
    pre-build:
      - echo pre-build
    build:
      - echo build
    post-build:
      - echo post-build
run: 
  pre-run:
    - echo pre-run
  runtime-version: 3.11
  command: python3.11 server.py
